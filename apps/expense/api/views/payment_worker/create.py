# recuerda que haqu son 2 create 
# el monto del pago se hace por el dato del USER MODEL campo payment_worker por defecto 0, deben de ser los usuarios is_staff con el campo mayor a 0
# 1- crear normal con generics.CreateApiView
# 2- va a tener que ser un API view normal porque la logica es que cree todos los payment worker